var array = new Array(7,9,3,2,45);


function sumaTotal(total, num) {
    return total + num;
}

function sumaTotal(total, num) {
    return total+num;
}


document.write(array.reduce(sumaTotal)/array.length+1);

